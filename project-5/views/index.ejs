<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BookMyShow</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    /* ---------- BODY ---------- */
    body {
      background: #1f1f2f;
      font-family: "Poppins", sans-serif;
      color: #fff;
      overflow-x: hidden;
    }

    /* ---------- NAVBAR ---------- */
    .navbar {
      background: rgba(30,30,50,0.85);
      backdrop-filter: blur(15px);
      padding: 1rem 2rem;
    }

    .navbar-brand {
      font-size: 1.8rem;
      font-weight: 700;
      color: #ff6f61 !important;
      letter-spacing: 1px;
    }

    .nav-link {
      color: #ccc !important;
      margin-right: 1.2rem;
      font-weight: 500;
      transition: 0.3s;
    }

    .nav-link:hover {
      color: #ff6f61 !important;
    }

    .search-box input {
      border-radius: 30px;
      border: none;
      padding: 8px 15px;
      background: rgba(255,255,255,0.1);
      color: #fff;
    }

    .search-box input::placeholder {
      color: #ccc;
    }

    .btn-search {
      border-radius: 30px;
      border: none;
      background: #ff6f61;
      color: #fff;
      font-weight: 500;
      padding: 8px 18px;
      transition: 0.3s;
    }

    .btn-search:hover {
      background: #e85b50;
    }

    /* ---------- SLIDER ---------- */
    .carousel-item img {
      height: 400px;
      object-fit: cover;
      filter: brightness(0.7);
    }

    /* ---------- SECTION HEADER ---------- */
    .section-header {
      text-align: center;
      margin: 50px 0 30px;
    }

    .section-header h1 {
      font-size: 2.5rem;
      color: #ff6f61;
      font-weight: 700;
      text-shadow: 0 0 10px #140e0d, 0 0 20px #181212;
    }

    /* ---------- MOVIE GRID ---------- */
    .movie-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 35px;
      padding: 0 50px 50px;
    }

    /* ---------- MOVIE CARD ---------- */
    .movie-card {
      background: linear-gradient(145deg, #2a2a3d, #1f1f2f);
      border-radius: 25px;
      overflow: hidden;
      box-shadow: 8px 8px 20px #111122, -8px -8px 20px #33334d;
      transition: transform 0.4s, box-shadow 0.4s;
      position: relative;
    }

    .movie-card:hover {
      transform: translateY(-10px) scale(1.02);
      box-shadow: 12px 12px 25px #111122, -12px -12px 25px #444466, 0 0 20px #ff6f61aa inset;
    }

    .movie-poster {
      width: 100%;
      height: 360px;
      object-fit: cover;
      transition: transform 0.4s, filter 0.4s;
    }

    .movie-card:hover .movie-poster {
      transform: scale(1.08);
      filter: brightness(1.1);
    }

    .movie-rating {
      position: absolute;
      top: 15px;
      right: 15px;
      background: #ff6f61;
      padding: 6px 12px;
      border-radius: 12px;
      font-weight: 700;
      color: #fff;
      font-size: 0.85rem;
      display: flex;
      align-items: center;
      gap: 5px;
      box-shadow: 0 0 10px #ff6f61aa;
      z-index: 2;
    }

    .movie-info {
      padding: 15px 20px 10px;
    }

    .movie-title {
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 8px;
      color: #fff;
      text-shadow: 0 0 8px #ff6f61;
    }

    .movie-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      font-size: 0.85rem;
      color: #ccc;
      margin-bottom: 8px;
    }

    .meta-item {
      background: rgba(255,255,255,0.05);
      padding: 5px 12px;
      border-radius: 15px;
      backdrop-filter: blur(5px);
      box-shadow: 2px 2px 5px #111122, -2px -2px 5px #33334d;
    }

    .movie-age {
      display: inline-block;
      background: #ff6f61;
      color: #fff;
      font-size: 0.75rem;
      padding: 4px 10px;
      border-radius: 15px;
      font-weight: 600;
    }

    /* ---------- ACTION BUTTONS ---------- */
    .movie-actions {
      display: flex;
      justify-content: space-between;
      padding: 12px 20px 18px;
      gap: 10px;
    }

    .btn-action {
      flex: 1;
      border: none;
      border-radius: 15px;
      padding: 8px 12px;
      font-weight: 600;
      font-size: 0.85rem;
      transition: 0.3s;
      color: #fff;
      text-align: center;
      box-shadow: 0 0 8px #0005;
    }

    .btn-edit { background: #ffa500; box-shadow: 0 0 10px #ffa50066; }
    .btn-delete { background: #ff4c4c; box-shadow: 0 0 10px #ff4c4c66; }
    .btn-view { background: #28c76f; box-shadow: 0 0 10px #28c76f66; }

    .btn-edit:hover { box-shadow: 0 0 15px #ffb84d; transform: translateY(-2px);}
    .btn-delete:hover { box-shadow: 0 0 15px #ff6b6b; transform: translateY(-2px);}
    .btn-view:hover { box-shadow: 0 0 15px #32d47b; transform: translateY(-2px);}

    /* ---------- FOOTER ---------- */
    .footer {
      text-align: center;
      padding: 25px;
      background: rgba(255,255,255,0.05);
      color: #ccc;
      margin-top: 40px;
      font-size: 0.9rem;
    }

    .footer span { color: #ff6f61; font-weight: 600; }

    @media (max-width: 768px) {
      .movie-grid {
        grid-template-columns: 1fr 1fr;
      }
      .movie-actions {
        flex-direction: column;
      }
    }

    @media (max-width: 500px) {
      .movie-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fas fa-video me-2"></i>BookMyShow</a>
      <button class="navbar-toggler bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/add_movie">Add Movie</a></li>
        </ul>
        <form class="d-flex search-box" method="GET">
          <input class="form-control me-2" type="search" name="q" placeholder="Search Movies..." value="<%= searchQuery %>">
          <button class="btn btn-search"><i class="fas fa-search"></i></button>
        </form>
      </div>
    </div>
  </nav>

  <!-- SLIDER -->
  <div id="carouselExampleIndicators" class="carousel slide mt-5" data-bs-ride="carousel" data-bs-interval="3500">
    <div class="carousel-inner">
      <div class="carousel-item active">
        <img src="https://assets-in.bmscdn.com/promotions/cms/creatives/1758520057275_22ndseppostmalonedesktop.jpg" class="d-block w-100" alt="Banner 1">
      </div>
      <div class="carousel-item">
        <img src="https://assets-in.bmscdn.com/promotions/cms/creatives/1757148047631_6thsepplaycardsep25hpptmdesktop.jpg" class="d-block w-100" alt="Banner 2">
      </div>
      <div class="carousel-item">
        <img src="https://assets-in.bmscdn.com/promotions/cms/creatives/1759294586758_2ndoctweaponsdesktop.jpg" class="d-block w-100" alt="Banner 3">
      </div>
    </div>
  </div>

  <!-- MOVIES SECTION -->
  <div class="section-header">
    <h1>Now Showing</h1>
  </div>

  <div class="movie-grid">
    <% movies.forEach((movie) => { %>
      <div class="movie-card">
        <img src="<%= movie.image %>" alt="<%= movie.name %>" class="movie-poster">
        <div class="movie-rating"><i class="fas fa-star"></i> <%= movie.rating %>/10</div>
        <div class="movie-info">
          <h5 class="movie-title"><%= movie.name %></h5>
          <div class="movie-meta">
            <div class="meta-item">Status: <%= movie.status %></div>
            <div class="meta-item">Lang: <%= movie.language %></div>
            <div class="meta-item">Cat: <%= movie.category %></div>
          </div>
          <span class="movie-age">Age: <%= movie.ageValidation %></span>
        </div>
        <div class="movie-actions">
          <a href="/edit-movie/<%= movie._id %>" class="btn-action btn-edit"><i class="fas fa-edit"></i> Edit</a>
          <a href="/delete-movie/<%= movie._id %>" class="btn-action btn-delete"><i class="fas fa-trash"></i> Delete</a>
          <a href="/view-movie/<%= movie._id %>" class="btn-action btn-view"><i class="fas fa-eye"></i> View</a>
        </div>
      </div>
    <% }) %>
  </div>

  <!-- FOOTER -->
  <div class="footer">
    © 2025 <span>BookMyShow</span> — All rights reserved.
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
